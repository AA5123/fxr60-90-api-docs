# Example summary titles — `set_mode`

Only examples that need a title different from the filename are listed here.
Any file not listed keeps its filename-derived title (underscores become spaces).

### `request/CUSTOM_write_access_password.json`

| Field | Value |
|---|---|
| **Example name** | `CUSTOM_write_access_password` |
| **Summary title** | `Assign an access password (WRITE to RESERVED bank)` |
